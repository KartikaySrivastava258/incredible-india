OPENSEARCH_URL=http://localhost:9200
MATCHING_SERVICE_PORT=8002

# "opensearch" = real k-NN vector search (required for the hackathon demo).
# "memory"     = in-process cosine search, no Docker needed, for quick dev.
# Falls back to "memory" automatically if OpenSearch isn't reachable at startup.
MATCHING_BACKEND=opensearch
